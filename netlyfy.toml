[build]
command = "npm run build"
publish = "dist"

# Главное правило для SPA
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
force = true  # Важно: перезаписывает любые конфликты

# Для статики (CSS/JS/изображения)
[[redirects]]
from = "/assets/*"
to = "/assets/:splat"
status = 200
